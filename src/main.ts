export function score(_rolls: number[]) {
	//I want it to be defined properly. I currently want the outcome to be 0, like it's expected to be.
	return 0; //Make it so that it adds all the numbers up.
}





//Make a system that says X + X = A + X = A + X (etc), stop when you run out of numbers. A = Answer, X = A number.















